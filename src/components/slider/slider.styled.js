import styled from "styled-components";

export const SliderStyled = styled.div`
position: relative;
height: 400px;
width: ${props => props.width}px;
margin: 0 10px;
overflow: hidden;
border-radius: 10px;
`;
